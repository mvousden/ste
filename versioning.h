/* The build process must define STE_VER with some sensible versioning
 * information, but in case it doesn't, this header works as a sensible
 * fallback. */
#ifndef STE_VER
#define STE_VER "unknown"
#endif
