version: 2.1
jobs:
  build:
    machine:
      image: ubuntu-2004:202104-01
    steps:
      - checkout
      - run: "sudo apt update"
      - run: "sudo apt install libbsd-dev valgrind"
      - run: "make"
      - run: ./test.sh
      - store_artifacts:
          path: test_outputs
